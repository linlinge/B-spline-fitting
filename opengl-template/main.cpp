#include "my_opengl.h"
#include "main.h"
#include "b_spline.h"

int main(int argv, char** argc)
{
	OpenglInit(argv,argc);
	//vector<vector<float>> dat;
	//vector<float> line1, line2;
	//line1.push_back(1); line1.push_back(2); line1.push_back(3);
	//line2.push_back(4); line2.push_back(5); line2.push_back(6);
	//dat.push_back(line1); dat.push_back(line2);

	//for (auto& line : dat)
	//{
	//	for (auto& val : line)
	//		cout << val << " ";
	//	cout << endl;
	//}

	//tranpose(dat);

	//for (auto& line : dat)
	//{
	//	for (auto& val : line)
	//		cout << val << " ";
	//	cout << endl;
	//}
	//system("pause");
	return 0;
}
